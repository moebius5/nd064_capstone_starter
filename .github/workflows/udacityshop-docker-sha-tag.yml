name: Uda'CityShop [sha tag] - Package with Docker

on:
  push:
    branches:
      - 'master'

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
        name: Docker meta
        id: meta
        uses: docker/metadata-action@v3
        with:
          images: ${{ env.IMAGES }}
          tags: type=sha
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      -
        name: Build and push latest
        id: frontend_build
        uses: docker/build-push-action@v2
        with:
          context: ./src/frontend
          file: ./src/frontend/Dockerfile
          push: true
          tags: moebius5/frontend:${{ steps.meta.outputs.tags }}
          labels: moebius5/frontend:${{ steps.meta.outputs.labels }}
      -
        name: Build and push latest
        id: productcatalogservice_build
        uses: docker/build-push-action@v2
        with:
          context: ./src/productcatalogservice
          file: ./src/productcatalogservice/Dockerfile
          push: true
          tags: moebius5/productcatalogservice:${{ steps.meta.outputs.tags }}
          labels: moebius5/productcatalogservice:${{ steps.meta.outputs.labels }}
      -
        name: Build and push latest
        id: currencyservice_build
        uses: docker/build-push-action@v2
        with:
          context: ./src/currencyservice
          file: ./src/currencyservice/Dockerfile
          push: true
          tags: moebius5/currencyservice:${{ steps.meta.outputs.tags }}
          labels: moebius5/currencyservice:${{ steps.meta.outputs.labels }}
      -
        name: Build and push latest
        id: adservice_build
        uses: docker/build-push-action@v2
        with:
          context: ./src/adservice
          file: ./src/adservice/Dockerfile
          push: true
          tags: moebius5/adservice:${{ steps.meta.outputs.tags }}
          labels: moebius5/adservice:${{ steps.meta.outputs.labels }}
